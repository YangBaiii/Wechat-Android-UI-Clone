<!-- res/drawable/button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="30dp" /> <!-- This defines the corner radius -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Optional border for the button -->
</shape>
